116a117,137
> # Install the user service
> if [ -f /opt/pibuilder/user.service ]; then
>   cp /opt/pibuilder/user.service ${ROOT_DIR}/user.service
> fi
> 
> # Install the users application
> if [  -f /input/start.sh ]; then
>   echo "Installing user application"
>   APP="${ROOT_DIR}/opt/app"
>   mkdir -p ${APP}
>   cp -Rf /input/* "${APP}"
>   chmod 755 "${APP}/start.sh"
>   echo "Contents of /opt/app"
>   ls -las ${APP}
> fi
> 
> LOCAL="/media/rpi_root/etc/rc.local"
> echo "-----------"
> cat ${LOCAL}
> echo "-----------"
> 
